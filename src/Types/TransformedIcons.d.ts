declare namespace TransformedIconsNS {

    interface ICommonIconProps {
        height ? : string | number ;
        width ? : string  | number;
        backgroundColor ? : UtilNS.AllowedColors;
        fill ?: any;
    }
  

}